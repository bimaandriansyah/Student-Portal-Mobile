<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pb.pkm.pnm.ac.studentportal">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".EditDataOrtuActivity"></activity>
        <activity android:name=".EditDataDiriActivity" />
        <activity android:name=".WisudaActivity" />
        <activity android:name=".DafUlActivity" />
        <activity android:name=".PengumumanActivity" />
        <activity android:name=".KuesionerActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".KuliahActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".TestingActivity">

        </activity>
        <activity android:name=".AktivasiActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>